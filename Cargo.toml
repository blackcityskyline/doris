[package]
name = "doris"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
ratatui = "0.29"
crossterm = "0.28"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
reqwest = { version = "0.12", default-features = false, features = ["json", "multipart", "cookies", "rustls-tls"] }
scraper = "0.21"
axum = "0.7"
encoding_rs = "0.8"
tracing = "0.1"
tracing-subscriber = "0.3"
anyhow = "1"
dirs = "5"
url = "2"
urlencoding = "2"
chrono = "0.4"
which = "6"
serde_json = "1"
futures-lite = "2"
fantoccini = "0.22.1"
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }
ring = "0.17"
hostname = "0.4"
whoami = "1"
getrandom = "0.2"
base64 = "0.22"
serial_test = "3"
rusqlite = { version = "0.31", features = ["bundled"] }
unicode-width = "0.2"
# Needed so `Source` (src/search/source.rs) can be used as `Box<dyn Source>`
# in a registry: native async-fn-in-traits (stable since Rust 1.75) is not
# object-safe on its own, this crate boxes the futures so it is. Small,
# widely-used, proc-macro only (no extra runtime dependencies pulled in).
async-trait = "0.1"
